# Copy to .env and adjust — docker-compose reads this automatically.
# Quick toggle for the payment broker (pasarela-grupo-3).

# Payment broker /queue endpoint. Leave empty to simulate payments locally.
# Example (LAN): http://192.168.1.50:8001/queue
BROKER_URL=
